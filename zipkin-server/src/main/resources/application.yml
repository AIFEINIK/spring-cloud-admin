server:
  port: 9411

spring:
  application:
    name: zipkin-server

eureka:
  client:
    serviceUrl:
        defaultZone: http://localhost:9000/eureka/
management:
  metrics:
    web:
      server:
        auto-time-requests: false
logging:
  config: classpath:log4j2.xml

